/** Groups smaller than this are merged/omitted in the AI context. */
export const STANDAARD_MIN_GROEPSGROOTTE = 5;

/** Only e-mail addresses on these top-level domains are accepted in demo mode. */
export const DEMO_TOEGESTAAN_EMAIL_SUFFIX = '.example';
